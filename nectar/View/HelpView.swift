import SwiftUI

struct HelpView: View {
    var body: some View {
        Text("Help Section is under maintanaince :(")
    }
}

#Preview {
    HelpView()
        .environmentObject(AuthViewModel())
} 
